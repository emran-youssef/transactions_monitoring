services:
  mysql:
    image: mysql:8.0
    environment:
        MYSQL_ROOT_PASSWORD: "0000"
        MYSQL_DATABASE: transaction_db
        MYSQL_USER: "eyatrooz"
        MYSQL_PASSWORD: "0000"
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: